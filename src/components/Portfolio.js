import React from 'react';
import NavBar from './Navbar'

export default function Portfolio() {
    return (
        <div style={{display: 'flex', justifyContent: 'space-between'}}>
            <h1 style={{padding: 20}}>Michael Lin</h1>
            <NavBar />
        </div>
    );
};